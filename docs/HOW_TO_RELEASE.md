# Как создать GitHub Release v1.0.0

## ✅ Готово: код запушен в main

Все файлы уже в GitHub: https://github.com/Patkins93/rpi5-v2raya-transparent

---

## 📦 Создай Release (вручную на GitHub)

### 1. Открой страницу создания релиза

Перейди по ссылке:
https://github.com/Patkins93/rpi5-v2raya-transparent/releases/new

### 2. Заполни форму

**Choose a tag:**
- Введи: `v1.0.0`
- Нажми "Create new tag: v1.0.0 on publish"

**Target:**
- Оставь `main`

**Release title:**
```
v1.0.0 — Первый публичный релиз
```

**Describe this release:**

Скопируй содержимое файла `RELEASE_NOTES.md` (см. ниже) или используй этот текст:

```markdown
# 🎉 v1.0.0 — Первый публичный релиз

## Что это?

**Raspberry Pi 5 как селективный VPN-шлюз** для YouTube, Instagram и других сервисов. Настраивается **за 5 минут**, работает автоматически для всех устройств в сети.

---

## ⚡ Быстрый старт

### На Raspberry Pi:

\`\`\`bash
sudo apt-get update && sudo apt-get install -y git
git clone https://github.com/Patkins93/rpi5-v2raya-transparent.git
cd rpi5-v2raya-transparent
sudo chmod +x ./scripts/*.sh
sudo ./scripts/install.sh
\`\`\`

### В v2rayA (http://IP_PI:2017):
- Добавь VPN-сервер
- Включи **Transparent Proxy (TProxy)**, порт **52345**

### На роутере (DHCP):
- **Default Gateway** = IP Raspberry Pi
- **DNS** = IP Raspberry Pi

Готово! 🎉

---

## 📦 Что включено

- ✅ Pi-hole (DNS + блокировка рекламы)
- ✅ v2rayA (панель управления VPN с TProxy)
- ✅ Автоматический ipset для доменов (YouTube, Instagram, WhatsApp, Google)
- ✅ Скрипты установки, проверки и обновления
- ✅ Защита SSH (порт 22 исключён из TProxy)

---

## 🆕 Особенности v1.0.0

### Для новичков:
- **Установка одной командой** (`sudo ./scripts/install.sh`)
- **Автоматическая настройка** всех компонентов
- **Понятные адреса панелей** (Pi-hole: `:80/admin`, v2rayA: `:2017`)
- **Проверка работы** (`sudo ./scripts/quick-check.sh`)

### Для опытных:
- Гибкая настройка через переменные окружения (`TPROXY_PORT`, `LAN_CIDR`, `IPSET_NAME`)
- Поддержка динамической смены интерфейса (eth0/wlan0)
- ipset с большой ёмкостью (1М записей)
- Сохранение правил через `netfilter-persistent`

---

## 📝 Известные ограничения

- Требуется **Raspberry Pi OS (Debian/Ubuntu-based)**
- Работает **только с IPv4** (IPv6 в следующих версиях)
- **iOS Private Relay** может обходить локальный DNS (нужно отключить на устройстве)

---

## 🔧 Обновления

После обновления доменов в репо:

\`\`\`bash
cd rpi5-v2raya-transparent
git pull
sudo ./scripts/update.sh
\`\`\`

---

## 📚 Документация

- [README.md](README.md) — полная инструкция
- [docs/LEGACY_RECOVERY.md](docs/LEGACY_RECOVERY.md) — восстановление доступа

---

## 🛡️ Security

- ✅ Приватные ключи исключены из git
- ✅ `.env` для хранения паролей (не попадает в репо)
- ✅ Минимальные права для контейнеров

---

## 🙏 Благодарности

- Pi-hole team — за отличный DNS с блокировкой рекламы
- v2rayA team — за удобную панель управления VPN
- Сообществу Raspberry Pi

---

**Если проект помог — поставь ⭐!**
```

### 3. Опубликуй релиз

- Нажми **"Publish release"**

---

## ✅ Готово!

Теперь пользователи смогут скачать релиз:

```bash
wget https://github.com/Patkins93/rpi5-v2raya-transparent/archive/refs/tags/v1.0.0.tar.gz
tar -xzf v1.0.0.tar.gz
cd rpi5-v2raya-transparent-1.0.0
sudo chmod +x ./scripts/*.sh
sudo ./scripts/install.sh
```

---

## 🎉 Поздравляю с первым релизом!
